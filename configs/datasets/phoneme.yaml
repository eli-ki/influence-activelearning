# Hard multi-class benchmark (influence N/A — BoostIn binary-only; use baselines + badge)
max_pool_samples: 5400
initial_labeled_fraction: 0.008
batch_size: 30
n_rounds: 25
n_seeds: 3

lgbm:
  n_estimators: 60
  learning_rate: 0.08
  num_leaves: 31
