# Tiny ImageNet — ADS + RALIF (200 classes, 100k train)
max_pool_samples: 12000
initial_labeled_fraction: 0.05
batch_size: 80
n_rounds: 30

lgbm:
  n_estimators: 150
  learning_rate: 0.06
  num_leaves: 127
  min_child_samples: 30

oracle:
  subset_size: 40
